#similar to list 
#structure to store key info 
#create a tuple (cant change tuple ever)
coordinates = (4, 5)
print(coordinates[0])

#dif between list and tuple 
#cant change a tuple like you can a list
 #list = []
 #tuple = ()
